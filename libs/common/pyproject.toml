# libs/common/pyproject.toml
[project]
name = "common"
version = "0.1.0"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
    "numpy>=1.26",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"